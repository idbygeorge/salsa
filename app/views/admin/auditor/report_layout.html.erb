<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="description" content="">
	<meta name="author" content="">
	<link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.css">
	<link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">

	<title>
		SALSA Tracker: Dynamic updates from Canvas
	</title>

	<!-- Bootstrap core CSS -->
	<%= stylesheet_link_tag "dashboard", media: "all" %>

	<!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
	<!--[if lt IE 9]>
	<script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
	<script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
	<![endif]-->
</head>

<body data-base-url="{{ constant('URL') }}">
	<%# render 'admin/topbar' %>

	<div class="heading">
		<div class="container">
			<h1 class="navbar-header"><i class="fa fa-compass"></i> <%= @organization.name if @organization %> SALSA Tracker</h1>

			<% if false %>
			<div class="col-sm-3 col-md-3">
				<br>
				<%= form_tag(auditor_reports_path, method: 'get', class: "form-horizontal col-md-9 navbar-form", role: "search" ) do |f| %>

				<div class="input-group">
					<input type="text" class="form-control" placeholder="Start Date (YYYY-MM-DD)" name="start" id="start-term" value="<%= params[:start] %>">
					<div class="input-group-btn">
						<button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-calendar"></i></button>
					</div>
				</div>
				<% end %>
			</div>
			<% end %>

			<ul class="nav navbar-nav navbar-right">
				<% if has_role 'organization_admin' %>
					<li><%= link_to 'Admin', admin_path, class: "btn btn-default" %></li>
				<% end %>

				<li><%= link_to 'Logout', admin_logout_path, class: "btn btn-default" %></li>
			</ul>
		</div><!-- .container  -->
	</div><!-- .heading -->


	<% if @messages %>
	<div class="container" id="messages">
		<% if messages.error %>
		<div class="alert alert-danger">
			<%= messages.error %>
		</div><!-- .alert-danger -->
		<% end %>
	</div> <!-- #messages -->
	<% end %>

	<%= render 'masthead' %>

	<div class="container" role="main">
		<%= yield %>
	</div><!-- .container[main] -->
	<!-- Bootstrap core JavaScript
	================================================== -->
	<!-- Placed at the end of the document so the pages load faster -->
	<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
	<script src="//maxcdn.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
	<%= javascript_include_tag "dashboard" %>
	<script src="https://code.highcharts.com/highcharts.js"></script>
	</body>
	</html>
