<h1 class="page-header">
  <%= @user[:name] %>
</h1>

<div id="adminUserControl row">
  <p>
  <a class="btn btn-default" href="<%= edit_admin_user_path(id: @user[:id]) %>">
      Edit User
    </a>

    <a class="btn btn-success pull-right" href="">
      <span class="glyphicon glyphicon-plus"></span>
      Add Access
    </a>

  </p>
  <hr>
</div>

<div class="row">
  <table class="table table-striped table-responive">
    <thead>
      <tr>
        <th>LMS User</th>
        <th>Role</th>
        <th>Organization</th>
        <th>Cascades</th>
        <th></th>
      </tr>
    </thead>

    <% @user.user_assignments.each do |ua| %>
      <%= render partial: 'role', locals: {role:ua} %>
    <% end %>
  </table>
</div>
